import React from 'react'
import { Release } from '@/components/component/release'

const FutureSoftwareRelease = () => {
  return (
      <Release />
   
  );
};

export default FutureSoftwareRelease;
